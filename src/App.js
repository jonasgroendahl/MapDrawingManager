import React from "react";
import MapDrawingManagerComponent from "./MapDrawingManager.js";

export default class MapDrawingManager extends React.Component {
  render() {
    return (
      <div>
        <MapDrawingManagerComponent />
      </div>
    );
  }
}
